# Script results

The results shown here were obtained by the GitHub Actions run in:

- **PR #447** (CUSTOM-CI)

The results will be updated by any subsequent pull request. Summary results are shown as a comment in the corresponding pull request.

Two files here are the exception: `macaw_results.csv` and `macaw_edge_list.csv`
are **not** regenerated by CI, because MACAW's dilution test takes too long to
run per pull request. They are only as current as the last manual
`python scripts/run_macaw.py`.